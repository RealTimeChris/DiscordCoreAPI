version: 2.1

orbs: 
  cmake: sensu/cmake@1.0.0


jobs:
  build:
    docker:
      - image: cimg/base:edge-22.04

    steps:
      - checkout
      - run:
          name: Install CMAKE
          command: sudo apt list --all-versions cmake
      - run:
          name: Install proper Curl.
          command: 'sudo snap remove curl && sudo apt install curl'
      - run:
          name: Installing build dependencies
          command: 'sudo apt-get update && sudo apt-get install -y sudo gcc g++ build-essential sudo git cmake libssl-dev zlib1g-dev'
      - run:
          name: Installing vcpkg
          command: 'sudo git clone https://github.com/Microsoft/vcpkg ./vcpkg && cd ./vcpkg && ./bootstrap-vcpkg.sh'
      - run:
          name: Creating Build Files
          command: 'cmake -S . -B ./Build'      
      - run:
          name: Creating Binary Files
          command: 'cmake --build --preset Linux-Release'
