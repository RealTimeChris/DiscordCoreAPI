@PACKAGE_INIT@

set(EXPORTED_TARGETS_FILENAME_NEW "@EXPORTED_TARGETS_FILENAME@")

set_and_check(EXPORT_TARGETS_NAME_NEW "${CMAKE_CURRENT_LIST_DIR}/${EXPORTED_TARGETS_FILENAME_NEW}")

if (WIN32)
	set_and_check(RELEASE_PDB_FILE "@PACKAGE_RELEASE_PDB_FILEPATH@")
	set_and_check(DEBUG_PDB_FILE "@PACKAGE_DEBUG_PDB_FILEPATH@")
endif()

include("${EXPORT_TARGETS_NAME_NEW}")

check_required_components(DiscordCoreAPI_CMake)
